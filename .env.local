# Local Development Environment Variables

# Environment Type
ENVIRONMENT=local
DJ_ENV=development
NODE_ENV=development

# Database Configuration
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Django Backend
DJ_DEBUG=True
DJ_SECRET=dev-secret-key-not-for-production
DJANGO_LOG_LEVEL=DEBUG
DJ_SAME_SITE=Lax
PYTHONPATH=/app

# Frontend & CORS
FRONTEND_URL=http://localhost:4200
API_URL=http://localhost:8000
CORS_ALLOWED_ORIGINS=http://localhost:4200

# Django Database
PGDATABASE=mzaker
POSTGRES_DB=${PGDATABASE}
PGUSER=${POSTGRES_USER}
PGPASSWORD=${POSTGRES_PASSWORD}
PGHOST=${POSTGRES_HOST}
PGPORT=${POSTGRES_PORT}
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}}

# Airflow Configuration
AIRFLOW__CORE__EXECUTOR=LocalExecutor
AIRFLOW__CORE__AUTH_MANAGER=airflow.providers.fab.auth_manager.fab_auth_manager.FabAuthManager
AIRFLOW_DATABASE_NAME=airflow
AIRFLOW__DATABASE__SQL_ALCHEMY_CONN=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${AIRFLOW_DATABASE_NAME}
AIRFLOW__CORE__FERNET_KEY=UKMzEm3yIuFYEq1y3-2FxPNWSVwRASpahmQ9kQfYVLA=
AIRFLOW__CORE__LOAD_EXAMPLES=false
AIRFLOW__API__AUTH_BACKENDS=airflow.api.auth.backend.basic_auth
AIRFLOW__API__SECRET_KEY=a-really-really-really-really-long-string-here
AIRFLOW_WEBSERVER_PORT=8080

# Default user (custom vars for start.sh)
AIRFLOW_DEFAULT_USER=airflow
AIRFLOW_DEFAULT_PASSWORD=airflow
AIRFLOW_DEFAULT_EMAIL=admin@example.com
AIRFLOW_DEFAULT_FIRSTNAME=Admin
AIRFLOW_DEFAULT_LASTNAME=User
AIRFLOW_DEFAULT_ROLE=Admin


# MinIO (Local S3)
MINIO_ROOT_USER=minio
MINIO_ROOT_PASSWORD=miniominio
MINIO_CONSOLE_PORT=9001
MINIO_API_PORT=9000
AWS_ACCESS_KEY_ID=minioaccess
AWS_SECRET_ACCESS_KEY=miniosecret
AWS_DEFAULT_REGION=us-east-1
S3_ENDPOINT_URL=http://minio:9000

# DuckDB & Data Pipeline
DUCKDB_PATH=/opt/airflow/duckdb/stocks.db
RAW_DATA_BUCKET=stock-insights-raw
PROCESSED_DATA_BUCKET=stock-insights-processed

# Service Ports
BACKEND_PORT=8000
FRONTEND_PORT=4200
